"
This class is obsolete, but exists only so that people using the old name will not have to change their code.
"
Class {
	#name : #BasicDictionaryMapping,
	#superclass : #DictionaryMapping,
	#category : 'Glorp-Mappings'
}
